import 'package:flutter/material.dart';

class ChatHomeBody extends StatelessWidget {
  const ChatHomeBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
